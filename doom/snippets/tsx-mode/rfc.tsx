# name: React Functional Component
# type: snippet
# --
import React from 'react';

export type $1Props = {$2};

export default function ${1:`(file-name-base (buffer-file-name))`}({$3}: $1Props) {
    $0
}
